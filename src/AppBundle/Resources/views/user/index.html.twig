{% extends '::base.html.twig' %}
{% block title %}CJ Parametre compte{% endblock %}
{% block body %}
    <nav>
        <div class="nav-wrapper">
            <div class="col s12">
                <a href="{{ path('home') }}" class="breadcrumb">Home</a>
                <a href="{{ path('customer_index') }}" class="breadcrumb">Liste clients</a>
                <a href="{{ path('customer_create') }}" class="breadcrumb">creation client</a>
            </div>
        </div>
    </nav>
    {#{{ dump(user) }}#}
    <div class="card-panel pad-T-0 mgr-R-15 mgr-L-15">
        <span class="blue-text text-darken-2 f-s-30 ">Parametres utilisateur</span>
    </div>
    <div class="container">
        <div class="row">
            <div class="center-align">
                <span>{{ user.username }}</span>
                <span><i class="material-icons">edit</i></span>
            </div>
        </div>
        <div class="row">
            <div class="center-align">
                <span>{{ user.email }}</span>
                <span><i class="material-icons">edit</i></span>
            </div>
        </div>
            <div class="row">
                <div class="center-align">
                    <a href="#">changer mp</a>
            </div>
        </div>
    </div>
{% endblock %}
{% block more_js %}
{% endblock %}