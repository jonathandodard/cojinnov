{% extends '::base.html.twig' %}
{% block title %}CJ Liste Client{% endblock %}
{% block body %}
    <nav>
        <div class="nav-wrapper">
            <div class="col s12">
                <a href="{{ path('home') }}" class="breadcrumb">{{ 'home'|trans|capitalize }}</a>
                <a href="{{ path('customer_index') }}" class="breadcrumb">{{ 'customer_list'|trans|capitalize }}</a>
            </div>
        </div>
    </nav>
    <div class="center-align search-content">
        <input placeholder={{ 'search'|trans|capitalize }} class="search" type="text" style="width: 50%" data-url="{{ path('customer_search') }}" data-type="customer"><i class="material-icons search-icon">search</i>
    </div>
    <a class="waves-effect waves-light btn-large" href="{{ path('customer_create') }}"><i class="material-icons left">person_add</i>{{ 'customer_creation'|trans|capitalize }}</a>
    <a class="waves-effect waves-light btn-large float-r" href="{{ path('import_cutomer') }}"><i class="material-icons left">file_upload</i>{{ 'import_customers'|trans|capitalize }}</a>
    {#<a class="waves-effect waves-light btn-large float-r" href="{{ path('customer_index') }}"><i class="material-icons left">cloud</i>supprimer selection</a>#}

    {% if customers != null %}
    <table class="responsive-table striped">
        <thead>
            <tr>
                <th class="center co-js-sort" data-url="{{ path("customer_sort") }}" data-sort='numberAccount' >{{ 'account_number'|trans|capitalize }}</th>
                <th class="center co-js-sort" data-url="{{ path("customer_sort") }}" data-sort='entitled' >{{ 'entitled'|trans|capitalize }}</th>
                <th class="center co-js-sort" data-url="{{ path("customer_sort") }}" data-sort='ranking' >{{ 'ranking'|trans|capitalize }}</th>
                <th class="center co-js-sort" data-url="{{ path("customer_sort") }}" data-sort='nameRepresentative' >{{ 'name_of_the_representative'|trans|capitalize }}</th>
                <th class="center co-js-sort" data-url="{{ path("customer_sort") }}" data-sort='name' >{{ 'contact'|trans|capitalize }}</th>
                <th class="center co-js-sort" data-url="{{ path("customer_sort") }}" data-sort='email' >{{ 'email'|trans|capitalize }}</th>
                <th class="center co-js-sort" data-url="{{ path("customer_sort") }}" data-sort='address' >{{ 'adress'|trans|capitalize }}</th>
                <th class="center co-js-sort" data-url="{{ path("customer_sort") }}" data-sort='additionalAddress' >{{ 'additional_address'|trans|capitalize }}</th>
                <th class="center co-js-sort" data-url="{{ path("customer_sort") }}" data-sort='zipCode' >{{ 'zip_code'|trans|capitalize }}</th>
                <th class="center co-js-sort" data-url="{{ path("customer_sort") }}" data-sort='city' >{{ 'city'|trans|capitalize }}</th>
                <th class="center co-js-sort" data-url="{{ path("customer_sort") }}" data-sort='phoneNumber' >{{ 'phoneNumber'|trans|capitalize }}</th>
                <th class="center"></th>
            </tr>
        </thead>
        <tbody class="co-js-search">
        {% for customer in customers %}
            <tr class="co-js-remove-sort">
                <td class="center"><a href="{{ path('customer_index_by_costumer', {'id': customer.id}) }}">{{ customer.numberAccount }}</a></td>
                <td class="center">{{ customer.entitled }}</td>
                <td class="center">{{ customer.ranking }}</td>
                <td class="center">{{ customer.nameRepresentative }}</td>
                <td class="center">{{ customer.name }}</td>
                <td class="center">{{ customer.email }}</td>
                <td class="center">{{ customer.address }}</td>
                <td class="center">{{ customer.additionalAddress }}</td>
                <td class="center">{{ customer.zipCode }}</td>
                <td class="center">{{ customer.city }}</td>
                <td class="center">{{ customer.phoneNumber }}</td>
                <td class="center">
                    <a href="{{ path('customer_update', {'id': customer.id })  }}"><i class="material-icons left">edit</i></a>
                    <a href="{{ path('customer_delete', {'id': customer.id })  }}"><i class="material-icons left">delete</i></a>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {% else %}
        <div class="row s12 mgr-T-150">

        <div class="center-align">
            <span class="blue-text text-darken-2 f-s-25">{{ 'you_do_not_have_a_customer_register'|trans|capitalize }}Vous n'avez pas de client enregister</span>
        </div>

    {% endif %}
    {#<ul class="pagination center-align">#}
        {#<li class="disabled"><a href="#!"><i class="material-icons">chevron_left</i></a></li>#}
        {#<li class="active"><a href="#!">1</a></li>#}
        {#<li class="waves-effect"><a href="#!">2</a></li>#}
        {#<li class="waves-effect"><a href="#!">3</a></li>#}
        {#<li class="waves-effect"><a href="#!">4</a></li>#}
        {#<li class="waves-effect"><a href="#!">5</a></li>#}
        {#<li class="waves-effect"><a href="#!"><i class="material-icons">chevron_right</i></a></li>#}
    {#</ul>#}
{% endblock %}